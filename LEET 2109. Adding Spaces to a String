class Solution {
    public String addSpaces(String s, int[] spaces) {
        String s1="";
        int j=0;
        int m=spaces.length;
for(int i=0;i<s.length();i++){
   if(j<m && i==spaces[j]){
    s1+=" ";
    j++;
   } s1+=s.charAt(i);
}
return s1;
    }
}
